# Handoff — 2026-05-03 pre-SSD-swap shutdown

Session goal: send clean upstream PRs to `quickwit-oss/tantivy` so the fork eventually converges. State frozen for SSD swap.

## ✅ Shipped

- **Upstream PR #2920** OPEN, MERGEABLE
  - https://github.com/quickwit-oss/tantivy/pull/2920
  - title: `feat(index): Index::rewrite_schema_on_disk for additive schema evolution`
  - +146 / -0, 1 file (`src/index/index.rs`), 2 unit tests
  - author: `Youichi Uda <youichi.uda@gmail.com>`

## 📦 Prepared, not yet submitted (4 PRs)

All on fork as backup branches; worktrees on disk are convenient but not required (branches are recoverable from fork).

| # | branch on fork | files / LOC | tests | PR body file |
|---|---|---|---|---|
| 2 | `pr/store-poison-recovery` | reader.rs +55/-4 | 3/3 | `PRs/02-store-poison-recovery.md` |
| 3 | `pr/range-query-i64-u64` | range_query_fastfield.rs +33/-7 (**EmptyScorer fix only**, i64/u64 part dropped — depends on BlockWAND fork-only file) | 30/30 | `PRs/03-range-query-i64-u64.md` |
| 4 | `pr/search-after-cursor` | 4 files +197/-2 | 89/89 | `PRs/04-search-after-cursor.md` |
| 5 | `pr/phrase-ordered-flag` | 4 files +473/-15 | 31/31 (caught real `u32::MAX` overflow bug, fixed in same commit) | `PRs/05-phrase-ordered-flag.md` |

PR body files are **inside the WIP backup branch** `wip/2026-05-03-handoff-backup` on fork (so they survive disk swap).

## 🔁 Resume strategy

When SSD swap is done and you're back:

1. **Check PR #2920 reception** first:
   ```bash
   env -u GH_TOKEN gh pr view 2920 --repo quickwit-oss/tantivy \
     --json reviewDecision,comments,mergedAt,statusCheckRollup -q .
   ```
2. Decide based on reception:
   - merged or positive review → push #2 next, then #3-#5 sequentially with 5-7 day spacing
   - silence (1 week) → polite ping comment on #2920
   - silence (2-3 weeks) → pivot to Phase B (proposal issues for Tier 2/3 patches)
3. **Recover the worktrees** if needed:
   ```bash
   cd /home/y1/git/ferroSearchProjects/tantivy
   git fetch origin
   for b in pr/store-poison-recovery pr/range-query-i64-u64 pr/search-after-cursor pr/phrase-ordered-flag; do
     git worktree add ../tantivy-pr-${b##*/} "$b"
   done
   ```
4. **Recover the WIP state** (term_agg.rs test + PR body files + audit):
   ```bash
   cd /home/y1/git/ferroSearchProjects/tantivy
   git fetch origin wip/2026-05-03-handoff-backup
   git checkout origin/wip/2026-05-03-handoff-backup -- \
     PRs/ UPSTREAM_SYNC_2026_05_03.md src/aggregation/bucket/term_agg.rs
   ```

## 🚀 Push commands for the 4 pending PRs (already pushed as branches; create PR only)

```bash
cd /home/y1/git/ferroSearchProjects/tantivy
# extract clean body (drop YAML front-matter and submission checklist) into a temp file
# example for #2:
sed -n '/^# PR body/,/^# Submission checklist/p' PRs/02-store-poison-recovery.md \
  | sed '1d;$d' > /tmp/pr-02-body.md

env -u GH_TOKEN gh pr create \
  --repo quickwit-oss/tantivy \
  --base main \
  --head youichi-uda:pr/store-poison-recovery \
  --title "fix(store): recover from poisoned block cache mutex" \
  --body-file /tmp/pr-02-body.md
```

Repeat for #3, #4, #5 (titles in each PR body file).

## ⚠️ Auth note (gh credential helper)

`/home/y1/.local/bin/git-credential-gh-user` is per-host pinned to `masumi-ryugo`. For pushes to `youichi-uda/ferro-tantivy` use:

```bash
git -c "credential.https://github.com.helper=" \
    -c "credential.https://github.com.helper=!/home/y1/.local/bin/git-credential-gh-user youichi-uda" \
    push ...
```

For `gh pr create` use `env -u GH_TOKEN gh ...` so it falls back to `hosts.yml` active account (youichi-uda).

## 🔒 Pre-shutdown safety check (already done)

- KVM VMs: all `シャットオフ` (broker-1/2/3, ci-runner, client) — no in-flight DB writes
- systemd-user fuzz/soak timers: none active in this session
- Background processes: no cargo / fuzz / broker / ferro-* processes
- Git working trees: all 5 PR worktrees clean; main worktree state captured in `wip/2026-05-03-handoff-backup`

## Worktrees on disk (recoverable from fork)

```
~/git/ferroSearchProjects/tantivy                main → 785884fd5
~/git/ferroSearchProjects/tantivy-pr-rewrite-schema   pr/rewrite-schema-on-disk → 275c38de4 (PR #2920)
~/git/ferroSearchProjects/tantivy-pr-store-poison     pr/store-poison-recovery → 9fbac7cb2
~/git/ferroSearchProjects/tantivy-pr-frangequery      pr/range-query-i64-u64 → a0695708b
~/git/ferroSearchProjects/tantivy-pr-search-after     pr/search-after-cursor → e2c2837cb
~/git/ferroSearchProjects/tantivy-pr-phrase-ordered   pr/phrase-ordered-flag → 210127381
```

Safe to delete worktrees post-reboot; recreate from fork branches as needed.

## Tier 2/3 — deferred work (Phase B)

Not started. Proposal issues to draft when Tier-1 reception is established:

- GPU crate (#26 + #1) — propose as optional workspace member
- BlockWAND (#13) + MaxScoreBulkScorer (#11) — likely obsolete; investigate adopting upstream's new `block_wand_intersection.rs` / `block_wand_union.rs` split; if fork additions remain valuable, propose what's missing
- Batch fast-field API (#20, #22, #4) + DocStore zero-copy (#14, #17) — wide API surface, propose first
- DateTime ns→us (f57fd1a98) — breaking; propose as opt-in or major-version
- Performance bundle (#24) — split per subsystem before any PR
